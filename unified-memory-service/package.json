{
  "name": "unified-memory-service",
  "version": "1.0.0",
  "description": "Unified Memory Service - Neural substrate for Chronos/Lantern/Resonance Family",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "test": "echo 'Tests coming soon'",
    "docker:build": "docker build -t unified-memory-service .",
    "docker:run": "docker run -p 3001:3001 unified-memory-service"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "sqlite3": "^5.1.6",
    "dotenv": "^16.0.3",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/cors": "^2.8.13",
    "@types/node": "^20.3.1",
    "typescript": "^5.1.6",
    "ts-node-dev": "^2.0.0"
  }
}
